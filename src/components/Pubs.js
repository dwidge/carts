import React from 'react'
import PropTypes from 'prop-types'

const Pubs = ({ stslots, stcarts, stpubs }) => {
	return (
		<div>
		</div>
	)
}

Pubs.propTypes = {
	stslots: PropTypes.array.isRequired,
	stcarts: PropTypes.array.isRequired,
	stpubs: PropTypes.array.isRequired,
}

export default Pubs
